import "./description.css"

function descriptions(props) {
    return (
        <div>What kinds of information should I display, what are the different paramters of props.</div>
    )
}

export default descriptions;